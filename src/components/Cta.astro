<!-- Final CTA Section -->
    <section class="py-20 bg-gray-50">
      <div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="text-4xl lg:text-5xl font-bold text-gray-900 mb-8">
          Empieza a usar DashUp gratis hoy mismo
        </h2>

        <div
          id="final-signup-form"
          class="flex justify-center mb-6">
         
      
          <button id="open-signup-modal"
            
            class="bg-blue-500 hover:bg-blue-700 text-white px-8 py-3 font-semibold rounded-md transition-colors"
          >
            Regístrate, ¡es gratis!
          </button>
        </div>

      
      </div>
    </section>
