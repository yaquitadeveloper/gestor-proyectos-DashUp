---
import { Icon } from 'astro-icon/components'


---

<!-- Hero Section -->
    <section
      class="relative overflow-hidden bg-gradient-to-br from-blue-50 to-purple-50"
    >
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20">
        <div class="grid lg:grid-cols-2 gap-12 items-center">
          <!-- Left content -->
          <div class="space-y-8">
            <h1
              class="text-5xl lg:text-6xl font-bold text-gray-900 leading-tight"
            >
              Captura, organiza y aborda tus tareas pendientes en cualquier
              lugar.
            </h1>

            <p class="text-xl text-gray-600 leading-relaxed">
              Escapa del desorden y el caos: da rienda suelta a tu productividad
              con DashUp.
            </p>

           
           
          </div>

          <!-- Right content - Phone mockup -->
          <div class="relative">
            <div class="relative z-50">
              <img
                src="/images/mobile02.png"
                alt="Trello mobile app"
                class="mx-auto max-w-sm"
              />
            </div>

            <!-- Decorative elements -->
            <div
              class="absolute top-10 right-10 w-20 h-20 bg-white  shadow-lg rounded-lg transform rotate-12 opacity-80 flex items-center justify-center"
            ><Icon name="slack" size={50} /></div>
            
            <div
              class="absolute bottom-20 right-0 w-16 h-16 bg-white shadow-lg rounded-full opacity-60 flex items-center justify-center"
            ><Icon name="meet" size={40} /></div>
            <div
              class="absolute top-1/2 -right-1 w-12 h-12 bg-white shadow-lg rounded transform -rotate-12 opacity-70 flex items-center justify-center"
            ><Icon name="gmail" size={50} /></div>

            <!-- App icons -->
            <div class="absolute top-8 -right-5 space-y-4">
              <div
                class="w-12 h-12 bg-white rounded-lg shadow-lg flex items-center justify-center"
              >
               <Icon name="twitter" size={40} />
              </div>
              <div
                class="w-12 h-12 bg-white rounded-lg shadow-lg flex items-center justify-center"
              >
                <Icon name="messenger" size={50} class="text-blue-500" />
              </div>
              <div
                class="w-12 h-12 bg-white rounded-lg shadow-lg flex items-center justify-center"
              >
                <Icon name="instagram" size={40} />
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>